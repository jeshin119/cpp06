{
	"version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "lldb",
            "request": "launch",
            "program": "${fileDirname}/a.out",
            "args": ["42.0f"],
            "cwd": "${fileDirname}/",
            "preLaunchTask": "C/C++: g++ build active file"
        }
    ]
}